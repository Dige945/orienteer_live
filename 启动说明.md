# 定向实时定位服务器启动说明

## 1. 启动网页服务器

打开 **Command Prompt**，进入项目目录：

```bat
cd /d D:\Code_House\orienteer
```

启动服务：

```bat
npm start
```

启动成功后，电脑本机访问：

```text
http://localhost:3000
```

如果手机和电脑在同一个 Wi-Fi，可以使用命令行里显示的 `LAN access` 地址，例如：

```text
http://你的电脑局域网IP:3000
```

## 2. 外网手机访问

如果别人手机不在同一个局域网，需要开内网穿透。

保持 `npm start` 不要关闭，再打开另一个 **Command Prompt**：

```bat
cloudflared tunnel --url http://localhost:3000
```

命令会生成一个公网地址，格式类似：

```text
https://xxxx.trycloudflare.com
```

别人手机访问这个地址，就能打开网页。

## 3. 正式使用流程

1. 启动服务器：

```bat
npm start
```

2. 打开网页：

```text
http://localhost:3000
```

3. 创建/替换当前赛事。

4. 上传定向底图图片。

5. 缩放、拖动、旋转定向底图，让它和公共地图对齐。

6. 点击“保存对齐”。

7. 如果需要外网访问，启动 Cloudflare Tunnel：

```bat
cloudflared tunnel --url http://localhost:3000
```

8. 用公网地址打开网页，在“手机接入”里确认服务器地址。

9. 手机 App 扫网页里的二维码，填写姓名，开始定位。

10. 网页切到“实时大屏”，查看每个人的位置和最近 20 秒拖痕。

## 4. 注意事项

- 当前系统是单赛事模式：每次新建赛事都会替换旧赛事，并清空旧参赛者、旧轨迹和旧底图记录。
- 服务器窗口不能关闭，关闭后网页和手机都会断开。
- Cloudflare Tunnel 窗口也不能关闭，关闭后公网地址会失效。
- 手机息屏定位需要系统允许定位权限和后台运行权限。
- 如果手机连接失败，优先检查服务器地址、赛事码、网络和内网穿透是否还在运行。
